describe('Note detail view', function() {

  beforeEach(function() {
    browser.get('app/#/notes/note-2');
  });


  it('should display placeholder page with phoneId', function() {
    //expect(element(by.binding('phoneId')).getText()).toBe('nexus-s');
  });
});
